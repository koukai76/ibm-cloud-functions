{
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "d": "npm run build && serverless deploy",
    "r": "serverless remove"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "serverless": "^2.31.0",
    "serverless-openwhisk": "^0.18.4",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0"
  }
}
